<%- include('templateTop') %>
    <h1><%= title %></h1>
    <form action="" method="POST">
        <div class="form-group">
            <label for="message">Message:</label>
            <input id="message" class="form-control" type="textarea" placeholder="Start typing!" name="message" required="true">
        </div>
        <button class="btn btn-primary" type="submit">Post Message</button>
    </form>
    <!-- < if(errors?.[Symbol.iterator]){ %>
        <ul>
            <for(const error of errors){ %>
                <li>
                    <=error.msg%>
                </li>
        </ul>
    < }} %> -->
<%- include('templateBottom') %>